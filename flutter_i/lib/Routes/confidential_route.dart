import 'package:flutter/material.dart';

class ConfidentialRoute extends StatelessWidget {
  const ConfidentialRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center());
  }
}
